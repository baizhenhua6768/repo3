<?xml version="1.0" encoding="UTF-8" ?>
<sqlMap namespace="custom" jdbcTimeout="5">

    <sql id="queryTables">
        <![CDATA[
           show tables
        ]]>
    </sql>
    
    <sql id="showColumnByTbName">
        <![CDATA[
           show columns from ${tableName} 
        ]]>
    </sql>
    
    
    <sql id="queryDataByTableName">
        <![CDATA[
           select * from ${tableName} 
           <#if field?? && field != ''>
				<#if value?? && value == '$null' >
					where ${field} is null
				<#elseif value?? && value == '$empty'>
					where ${field} = '';
				<#else>
					where ${field} = :value
				</#if>
           </#if>
           limit :start,:end
        ]]>
    </sql>
    
    <sql id="countDataByTableName">
        <![CDATA[
           select count(1) from ${tableName} 
           <#if field?? && field != ''>
             	<#if value?? && value == '$null' >
					where ${field} is null
				<#elseif value?? && value == '$empty'>
					where ${field} = '';
				<#else>
					where ${field} = :value
				</#if>
           </#if>
        ]]>
    </sql>
    
    <sql id="update">
        <![CDATA[
           update ${tableName} set
           	${field} = :fieldValue
           where ${pk} = :pkValue
        ]]>
    </sql>
    
    <sql id="delete">
        <![CDATA[
           delete from ${tableName} where ${pk} = :pkValue
        ]]>
    </sql>
</sqlMap>