<?xml version="1.0" encoding="UTF-8" ?>
<sqlMap namespace="staffBeStoreAddress" jdbcTimeout="5">
    <!--查询店员所属门店地址-->
    <sql id="queryStaffBeStoreAddressInfo">
        <![CDATA[
            SELECT
                d.NAME AS storeName,
                d.SHORT_NAME as shortName,
                d.ADDRESS as storeAddress
            FROM
                o2o_store_detail d
            WHERE
                d.STORE_CODE=:storeCode
        ]]>
    </sql>


    <!-- 根据门店编码查询门店信息 -->
    <sql id="queryBusinessType">
        <![CDATA[
            SELECT
				business_type AS businessType
			FROM
				o2o_v_guide_info
			WHERE
			    GUIDE_ID = :staffId
        ]]>
    </sql>

    <!-- 根据店员工号查询门店编码 -->
    <sql id="queryStoreCodeByStaffId">
        <![CDATA[
            SELECT
				STORE_CODE
			FROM
				o2o_v_guide_info
			WHERE
			    GUIDE_ID = :staffId
        ]]>
    </sql>

    <!-- 查询客户经理 -->
    <sql id="queryManagerInfoNew">
        <![CDATA[
            SELECT staff_id from o2o_staff_cust_rel
			WHERE
			    cust_no = :customerNo and business_type=:businessType and delete_flag='0'
        ]]>
    </sql>


</sqlMap>